// import { Router } from "express";

// import * as PermissionController from "../controllers/permission";
// import { checkError } from "../middleware/requireAuth";
// import { permission } from "../util/validators/validateData";

// const router = Router();

// router.get("/:id", PermissionController.getPermission);
// router.post("/", permission, checkError, PermissionController.addPermission);
// router.put("/:id", PermissionController.updatePermission);
// router.delete("/:id", PermissionController.deletePermission);
// export default router;
